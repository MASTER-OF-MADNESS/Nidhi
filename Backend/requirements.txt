# NIDHI — CSR Fund Intelligence Platform
# Python 3.10+
#
# Note: sqlite3 and uuid are Python standard library and are deliberately NOT
# listed here. The PyPI package named "uuid" is a broken 2006 backport that
# shadows the stdlib module if installed.

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
pydantic>=2.9.0

# --- AI / retrieval ---
google-genai>=1.0.0
tavily-python>=0.5.0
rank-bm25>=0.2.2

# --- Optimizer ---
ortools>=9.11.4210

# --- Markdown parsing ---
markdown>=3.7
python-frontmatter>=1.1.0

# --- IO ---
aiofiles>=24.1.0
httpx>=0.27.2

# --- Tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
